{% extends 'base.html.twig' %}

{% block title %}{{ 'title.profile'|trans }}{% endblock %}

{% block body %}
    {{ form_start(form) }}

    {{ form_row(form.display) }}
    {{ form_row(form.email) }}

    {{ form_row(form.plainPassword) }}

    <button type="submit" class="btn mt-2 btn-primary">{{ 'button.save'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}